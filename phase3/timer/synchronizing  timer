add>thread>threadgroup
  add>http request three times
1.blazedemo${__(yyyymmddhhmmss)}
2.heroku${__(yyyymmddhhmmss)}
3.herokulogin${__(yyyymmddhhmmss)}
add constant timer >give time
add>summary report
view the result 
